/**
 * 
 * @param {number} rad 
 * @returns {number}
 */
const radToDeg = (rad) => (rad / (PI * 2)) * 360

/**
 * 
 * @param {number} deg 
 * @returns {number}
 */
const degToRad = (deg) => (deg / 360) * PI * 2

