import { ArrayT } from "./arrays.js";

/**
 * @template T, U
 * @typedef {[T, U]} _Pair
 */

/***
 * @template T, U
 * @param {_Pair<T, U>} pairArr
 * @returns {_Pair<T, U>}
 */

export function Pair(pairArr) {
    return pairArr
}